# Pick from Friend table
# -----------------------------
# id | userId | Friend.json
#    | (自分) | （相手）
query friends($id: ID!) {
  friends(id: $id) {
    id
    name
    profileImageUrl
    lastAccessedAt
  }
}